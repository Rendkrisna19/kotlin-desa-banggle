<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F5F5F5"
    tools:context=".AdminLihatPengajuanActivity">

    <!-- Header Toolbar Admin -->
    <androidx.appcompat.widget.Toolbar
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="#1A237E"
        app:title="Semua Pengajuan Surat Warga"
        app:titleTextColor="#FFFFFF"/>

    <!-- 🔍 KOMPONEN FILTER SPINNER (Drop-down) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="12dp"
        android:background="#FFFFFF"
        android:elevation="2dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Filter Status: "
            android:textColor="#212121"
            android:textStyle="bold"
            android:layout_gravity="center_vertical"/>

        <Spinner
            android:id="@+id/spinnerFilterStatus"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:spinnerMode="dropdown"
            android:layout_gravity="center_vertical"/>
    </LinearLayout>

    <!-- Tempat List Semua Surat -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvAdminPengajuanSurat"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp"
        android:clipToPadding="false"
        tools:listitem="@layout/item_pengajuan_surat"/>

</LinearLayout>